---
labels: ['angular', 'typescript', 'demo-card-title']
description: 'A `demo-card-title` component.'
---

# DemoCardTitle documentation

Import `DemoCardTitleModule` into your application:

```ts
import { DemoCardTitleModule } from './demo-card-title.module';

// add it to your module imports
@NgModule({
  imports: [
    DemoCardTitleModule
  ]
})
export class AppModule {}
```

Use `DemoCardTitleComponent` in your templates:

```html
<demo-card-title></demo-card-title>
```
